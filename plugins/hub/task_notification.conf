# config file for the Koji task_notification plugin

[permissions]
# task methods for whose notification can be triggered
# * can be used to allow everything. Multiple values are delimited by comma.
allowed_methods = maven
allowed_states = FAILED,CANCELED
